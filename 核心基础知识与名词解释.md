# 平行志愿录取系统 · 核心基础知识与名词解释

> 用途：把项目里出现的名词和技术点一次讲清楚，面试被追问概念时可以直接查。

---

## 一、业务概念

### 平行志愿

在同一批次里，考生可以填报若干个并列的院校专业志愿（本项目是 6 个）。
投档时所有考生按排位排序，**分数优先、遵循志愿**：
排位高的考生先投，投档时从第 1 志愿开始依次检索，命中第一个仍有计划余额的志愿即投出，
投出后不再检索后面的志愿。这样能降低「高分落榜」的概率。

### 分数优先

投档顺序由排位决定，而不是先处理所有人的第一志愿再处理第二志愿。
也就是说，第 100 名考生的第 6 志愿，优先于第 101 名考生的第 1 志愿。

### 遵循志愿

对同一个考生来说，志愿是**有先后顺序**的：先看第 1 志愿有没有余额，再看第 2 个，依次类推。
一旦命中，后面的志愿全部作废，所以志愿填报顺序很关键。

### 排位（`rank`）

同一科类考生按分数从高到低的名次，数值越小排位越高。本项目 `t_student.rank` 保存排位，
投档时 `ORDER BY rank` 升序处理。

### 投档 vs 录取

- **投档**：把考生的档案投给某个专业，本项目里表现为写 `accepted_major_id`。
- **录取**：投档后由专业确认接收。本项目的匹配逻辑把两步合并成一次判断。

### 招生计划数 / 实际录取数

- `plan_student_count`：专业计划招多少人，投档上限。
- `realistic_student_count`：实际已经录了多少人，每次命中加一。
- 两者相等时，专业不再接收投档。

### 服从调剂（`adjust`）

考生的 6 个志愿全部落空时，如果 `adjust = 1`，就进入调剂池，由系统补录到还有缺额的专业；
如果 `adjust = 0`，直接退档。

### 退档

考生没有被任何专业录取。本项目里 `accepted_type = -1`，
`accepted_major_id` 为 NULL，退档名单可以单独导出。

### 录取状态码（`accepted_type`）

| 取值 | 含义 |
| --- | --- |
| `-2` | 初始值，尚未参与投档 |
| `-1` | 退档 |
| `0` | 服从调剂，等待调剂 |
| `1` ~ `6` | 被第 N 个志愿录取 |
| `7` | 被专业调剂录取 |

### 流程状态（`EnrollStatus`）

| 枚举 | 序号 | 含义 |
| --- | --- | --- |
| `START` | 0 | 初始或已重置 |
| `WITHOUT_STUDENT` | 1 | 已导入招生计划 |
| `FILE_READY` | 2 | 计划与志愿齐备，可投档 |
| `ENROLLED` | 3 | 已投档，可调剂 |
| `ADJUSTED` | 4 | 已调剂，可导出 |

---

## 二、Spring Boot

### 什么是 Spring Boot

在 Spring 基础上做了自动配置的框架：引入 `spring-boot-starter-web` 就自带内嵌 Tomcat，
打成可执行 jar 后 `java -jar` 就能跑，不需要单独部署到外部容器。

### 依赖注入（DI）与控制反转（IoC）

对象不由自己 `new`，而是交给 Spring 容器创建和管理，需要时用 `@Autowired` 注入。
好处是解耦，比如 `StudentServiceImpl` 只依赖 `StudentMapper` 接口，
换实现或写测试时不需要改业务代码。

### Bean 与容器

被 Spring 管理的对象叫 Bean。`@Component`、`@Service`、`@Controller`、`@Configuration`
标注的类会被扫描并注册进容器。

### `@Configuration` 与 `WebMvcConfigurer`

`@Configuration` 表示这是一个配置类。实现 `WebMvcConfigurer` 可以定制 MVC 行为，
本项目用它注册登录拦截器（`addInterceptors`）和跨域规则（`addCorsMappings`）。

### `HandlerInterceptor`

在请求进入 Controller 之前、之后插入逻辑的组件。
本项目的登录拦截器在 `preHandle` 里校验 Session，未登录直接中断请求。

### 全局异常处理（`@ControllerAdvice`）

集中处理所有 Controller 抛出的异常，避免每个接口都写 `try-catch`。
本项目用它把业务异常映射成 `code=001`，系统异常映射成 HTTP 500 + `code=100`。

### 事务（`@Transactional`）

标注的方法在一个数据库事务里执行，要么全部成功，要么全部回滚。
本项目的投档与调剂都在事务中，保证「考生状态回写」和「专业人数回写」不会只成功一半。

### 拦截器与过滤器（Filter）的区别

- Filter 属于 Servlet 规范，作用在请求进入 DispatcherServlet 之前，拿不到 Controller 信息。
- Interceptor 属于 Spring MVC，作用在 Handler 执行前后，可以拿到 HandlerMethod，
  也能访问 Spring 容器里的 Bean。

### Session 与会话固定攻击

Session 是服务端为每个浏览器会话保存的状态，浏览器通过 `JSESSIONID` Cookie 找到它。
会话固定攻击指攻击者先诱导用户使用一个已知的 SessionId，用户登录后该 Session 变成已登录状态，
攻击者就能复用这个 Id。防御方式是在登录成功后让旧 Session 失效并新建一个，
本项目在 `doLogin` 里就是这么做的。

---

## 三、MyBatis

### 什么是 MyBatis

半自动的持久层框架：SQL 由开发者写在 XML 或注解里，MyBatis 负责参数映射和结果映射。
相比全自动 ORM，SQL 完全可控，便于针对报表做优化。

### Mapper 接口与 XML 的绑定

接口方法名和 XML 里的 `id` 一一对应，`namespace` 必须等于接口全限定名。
本项目接口在 `org.enroll.mapper`，XML 在 `resources/mybatis/mapper/`。

### `#{}` 与 `${}` 的区别

- `#{}` 生成 JDBC 预编译占位符 `?`，参数作为值传入，能防止 SQL 注入。
- `${}` 是字符串直接拼接，能用于表名、排序字段这类无法预编译的场景，但有注入风险。

> 项目里修改招生计划数的 SQL 原来写的是 `WHERE major_id = ${majorId}`，
> 属于典型的注入风险，已改成 `#{}`。这也是一个很常见的面试追问点。

### 动态 SQL

`<if>`、`<where>`、`<foreach>`、`<trim>` 等标签用来按条件拼 SQL。
本项目用 `<foreach>` 做批量插入和批量更新，用 `<if>` 做条件筛选。

### 一对多 / 多对一映射

`<resultMap>` + `<association>` / `<collection>` 用于把关联查询结果映射到对象。
本项目的关系比较简单，主要用 `resultType` 直接映射到 POJO。

### 分页插件 PageHelper

`PageHelper.startPage(pageNum, pageSize)` 会对紧随其后的第一条查询自动加 `LIMIT`，
再包装成 `PageInfo` 返回总条数、总页数等信息。本项目每页 50 条。

### 逻辑删除与物理删除

- 物理删除：`DELETE FROM`，数据真的消失，本项目重置功能用的是它。
- 逻辑删除：加 `is_deleted` 标记，查询时过滤，数据可追溯。

---

## 四、MySQL

### InnoDB 与 MyISAM

InnoDB 支持事务、行级锁和外键，是 MySQL 5.5 之后的默认引擎；MyISAM 不支持事务。
本项目需要事务和外键，所以全部使用 InnoDB。

### 主键、外键与索引

- **主键**：唯一标识一行，本项目 `t_department.department_id` 用自增整数。
- **外键**：约束引用完整性。`t_major.department_id` 指向学院，`t_student.accepted_major_id` 指向专业。
- **索引**：加速查询的数据结构。本项目在 `rank` 上建索引，因为投档要按排位排序取数；
  在 `(accepted_major_id, rank)` 上建联合索引，用于按专业统计录取名单。

### 联合索引与最左前缀

联合索引 `(accepted_major_id, rank)` 能加速
「按专业查询」以及「按专业 + 排位范围查询」；
但只按 `rank` 查询用不到这个索引的最左列，所以另外单独建了 `idx_rank`。

### 事务与 ACID

- **原子性 A**：事务内的操作要么全成功，要么全回滚。
- **一致性 C**：事务前后数据完整性约束不被破坏。
- **隔离性 I**：并发事务之间互不干扰。
- **持久性 D**：提交后的数据不会丢失。

### 连接池与 Druid

建立数据库连接开销大，连接池预先维护一批连接重复使用。
Druid 除了池化，还提供慢 SQL 统计和监控页，本项目通过 `DRUID_CONSOLE_ENABLED` 控制是否开启。

### `LIMIT offset, size` 的性能问题

偏移量很大时，MySQL 仍要扫描并丢弃前 offset 行。
本项目投档时分页读取用的是 `LIMIT start, 200`，在几千条数据下没有问题；
数据量再大应改成「按主键游标翻页」（`WHERE id > lastId LIMIT 200`）。

### 时间精度陷阱

`datetime` 默认精度是秒（`datetime(0)`）。同一秒内插入多条记录时，
按时间排序的结果不确定。本项目状态查询因此改成
`ORDER BY log_time DESC, log_id DESC`。

---

## 五、EasyExcel

### 为什么不用 POI 直接读写

Apache POI 会把整个工作簿加载到内存，几万行就很容易 OOM。
EasyExcel 基于 SAX 逐行解析，内存占用基本恒定，适合批量导入。

### 监听器机制

`AnalysisEventListener<T>` 每读一行调用一次 `invoke`，读完后调用 `doAfterAllAnalysed`。
本项目在 `invoke` 里攒够 20 条就批量插入一次，兼顾内存和数据库往返次数。

### 关键注意点

- 监听器是**每次导入新建**的，缓存集合必须是实例字段；写成 `static` 会导致并发导入数据串号。
- 表头通过 `@ExcelProperty("列名")` 绑定字段，列顺序变化不影响导入。
- 导出时用 `ExcelWriter` 分页写，避免一次性构造全部结果对象。

---

## 六、接口与安全

### REST 与统一返回结构

接口用 HTTP 方法表达操作语义，返回体统一为
`{"code": "...", "data": ..., "message": "..."}`，前端只需判断 `code`。

### HTTP 状态码

| 状态码 | 场景 |
| --- | --- |
| 200 | 业务处理完成（`code` 再区分成功与业务校验失败） |
| 401 | 未登录 |
| 500 | 服务端未预期异常 |

### 环境变量注入配置

把密码写在代码或仓库里的配置文件，一旦推送就会泄露。
本项目所有敏感项都写成 `${MYSQL_PASSWORD:}` 这种占位形式，
运行时从环境变量读取，仓库里只有默认值或空值。

### 会话固定攻击与防御

见上文「Session 与会话固定攻击」。

### CORS 与凭证

浏览器默认禁止跨域携带 Cookie。后端开启 `allowCredentials(true)` 时，
`allowedOrigins` 不能写成 `*`，必须写具体来源（本项目是 `http://localhost:8000`）。

---

## 七、构建与测试

### Maven 生命周期

`validate → compile → test → package → verify → install → deploy`。
`mvn test` 跑到测试；`mvn package` 生成 jar。

### Maven 依赖范围（scope）

| scope | 作用范围 |
| --- | --- |
| `compile`（默认） | 编译、测试、运行都可用 |
| `runtime` | 编译不用，运行需要（如 MySQL 驱动） |
| `test` | 仅测试代码可用 |
| `provided` | 运行环境已提供（如部署到外部 Tomcat 时的 servlet-api） |

### JUnit 5 与 Spring Boot Test

- 普通单元测试直接 new 对象，不需要启动 Spring，毫秒级完成。
- `@SpringBootTest` 会启动完整容器，用于集成测试，适合验证 SQL 和事务。
- `@EnabledIfEnvironmentVariable` 可以让需要数据库的测试在缺少环境时自动跳过，
  避免本地没有数据库时构建失败。

### 为什么规则要单独测试

投档规则一旦出错，影响的是成千上万考生的录取结果。
纯函数版本的规则可以用极低成本覆盖所有分支，比手工点页面可靠得多。

---

## 八、快速复习表

| 名词 | 一句话解释 |
| --- | --- |
| 平行志愿 | 分数优先、遵循志愿，一次投档 |
| 排位 | 同科类按分数从高到低的名次 |
| 计划数 | 专业最多招多少人 |
| 服从调剂 | 志愿全落空后接受系统分配到缺额专业 |
| 退档 | 没有被任何专业录取 |
| Bean | 由 Spring 容器管理的对象 |
| IoC / DI | 对象交给容器创建，按需注入 |
| Interceptor | Spring MVC 层的请求拦截器 |
| `#{}` / `${}` | 预编译占位符 / 字符串拼接 |
| PageHelper | MyBatis 分页插件 |
| 联合索引 | 多列组成的索引，遵循最左前缀 |
| ACID | 事务的原子性、一致性、隔离性、持久性 |
| Druid | 数据库连接池 + 监控 |
| EasyExcel | 基于 SAX 的低内存 Excel 读写库 |
| `@Transactional` | 声明式事务 |
