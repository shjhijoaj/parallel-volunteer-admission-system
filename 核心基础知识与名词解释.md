# 课选核心知识

| 名词 | 在本项目里怎么理解 |
| --- | --- |
| HTTP / REST | 浏览器按 URL、方法、JSON 与后端交换数据；GET 查询，POST 提交，PUT 更新，DELETE 删除 |
| Spring Boot | 启动 Java 服务、管理对象和配置；不是分配算法本身 |
| Controller / Service / Mapper | 接请求 / 实现业务约束 / 读写数据库的职责划分 |
| Vue 响应式 | 数据改变后对应页面更新；后端仍须再次验证输入 |
| JDBC / MyBatis | Java 访问关系库的接口 / 将 SQL 和 Java 映射的工具 |
| H2 / MySQL | 当前默认文件数据库 / 可配置的独立数据库服务 |
| 主键 / 外键 / 唯一约束 | 唯一标识记录 / 保证关联目标存在 / 阻止重复报名 |
| 事务 | 要么整批成功、要么回滚；防止半导入、半分配 |
| 行锁 | 分配时让同批次的并发请求等待，串行判断状态 |
| 状态机 | 草稿、开放、关闭、已分配、已公示；只允许规定的转换 |
| Cookie / Session | 浏览器保存会话标识，服务端保存当前账号状态 |
| PBKDF2 / 盐 | 慢速口令派生与随机盐；避免明文存密码和相同密码相同摘要 |
| CSRF | 其他站点诱导浏览器携带 Cookie 写数据；随机 Token 与来源校验降低风险 |
| XSS | 将不可信内容当脚本执行；Vue 文本插值与避免 v-html 用于保持文本显示 |
| 单元测试 / 集成测试 | 独立测算法 / 联合检查 API、数据库、事务等真实组件 |

## 一条请求
学生点提交 → Vue 取得课程 ID 顺序 → 携带 Cookie/Token POST → 后端校验身份、时间、状态和意向 → 事务保存 → 返回成功 → 前端刷新。任何一步失败显示原因，不能只靠按钮变灰来保护数据。

## 学习小练习
1. 新增一条课程并在数据库找到它。
2. 把课程名额设为 1，用两人报名观察第二人结果。
3. 提交重复课程 ID，理解为什么服务端也要拒绝。
4. 在 apply 方法断点，逐行观察志愿如何转换为 JSON。
5. 单独跑并发分配测试，说明锁与状态检查缺一不可。
