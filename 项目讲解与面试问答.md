# 课选：项目讲解与面试问答

## 30 秒介绍
课选是面向学院实训周和工作坊的课程报名平台。学生先提交有顺序的意向，老师维护课程容量，关闭报名后统一分配。它解决的是多个课程名额有限、人工整理意向容易出错的问题。后端用 Spring Boot，前端 Vue 3，数据默认保存在本地 H2，支持 Excel 导入导出和完整的报名公示流程。

## 阅读顺序
先启动后用管理员和学生走一遍流程，再读 course-ui/app.js 的请求 → CourseController 的接口 → CourseService 的 apply/transition/allocate → CourseMapper 查询 → EnrollEngine 内核 → CourseWorkflowTest。先理解一个报名如何保存，再理解整批如何分配，不必一次背完源码。

## 常见追问
**为什么要统一分配？** 抢课只看点击速度；这个系统可按提前公布的优先分和意向排序，在同一截止点统一处理。默认同分按首次报名编号排序，不声称随机或绝对公平。

**为什么保留六个意向？** 沿用规则内核输入的上限，每人最多分配一门；后端同时检查课程归属和重复，前端只是提前提示。

**如何防止超额或重复分配？** 引擎按容量递减；事务锁定批次后检查状态。两次并发请求不会同时基于同一份 CLOSED 状态完成分配，第二个读到新状态后被拒绝。

**为什么用 H2？** 降低本机试用门槛，文件库可持久化，适合单实例演示与小规模场景；大规模部署应评估 MySQL、备份和容量，不能把 H2 测试直接说成 MySQL 生产验证。

**MyBatis 和 JdbcTemplate 为什么同时存在？** MyBatis 描述列表及关联查询，JdbcTemplate 写业务事务。两者使用同一 Spring 数据源和事务管理；这是实现取舍，不是必须混用的框架规定。

**导入一半出错怎么办？** Excel 先读取并验证所有行，再在事务中统一插入；任何一条不合法就不写本次数据。设置大小和行数限制，避免无限消耗内存。

**登录安全吗？** 密码使用带盐 PBKDF2 摘要；会话 ID 轮换；写接口带 CSRF Token；权限在服务端检查；改密使旧会话失效。公网部署仍需 HTTPS 和独立的安全评估。

**目前不足？** 每批最多一门、不跨批次检测时间冲突、没有学校 SSO 和自动候补；没有声称实际服务过学校。能说明这些约束比编造用户量更可信。

## 演示准备
准备两个浏览器、三门示例课、两个学生和小容量课程。展示第二志愿命中、选择调剂、禁止重复分配、公示前学生看不到结果。用本次真实测试结果介绍质量，不背未测过的指标。来源与授权见 CREDITS.md。
