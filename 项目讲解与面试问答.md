# 平行志愿录取系统 · 项目讲解与面试问答

> 用途：面试前 10 分钟快速回忆项目，以及被追问时的标准答法。
> 所有数字都可以在 `docs/evidence/verification-summary.json` 与 `docs/evidence/` 里找到原始证据。

---

## 一、项目一句话概括

一个用 Spring Boot + MyBatis + MySQL 实现的平行志愿录取系统，把招生计划和考生志愿通过 Excel 导入，
按「分数优先、遵循志愿」完成投档和专业调剂，最后输出录取结果、退档队列和统计报表。

## 二、30 秒版本（自我介绍里用）

> 我做过一个平行志愿录取系统，技术栈是 Spring Boot、MyBatis、MySQL 和 Vue。
> 核心是把投档规则抽成一个独立的规则引擎：考生按排位从高到低，依次检查 6 个志愿，
> 命中第一个还有计划余额的专业就投档，全部落空再看是否服从调剂。
> 我用示例数据跑了完整流程，6862 名考生、49 个专业、6666 个计划，
> 最终 6409 人按志愿录取、257 人调剂、196 人退档，零超录。

## 三、2 分钟版本

这个项目分三块。

第一块是数据导入。招生计划和考生志愿都是 Excel，用 EasyExcel 做流式读取，
监听器按 20 条一批写库，避免几万行数据一次性进内存。

第二块是核心业务。我把投档规则从 Service 里抽出来，写成一个叫 `EnrollEngine` 的纯函数类，
它不依赖 Spring 也不连数据库，只接收「考生 + 专业计划表」，返回「命中哪个志愿或退档」。
这样规则可以用单元测试全覆盖，我写了 8 个用例，覆盖第一志愿命中、前面志愿录满后顺延、
服从调剂、不服从退档，以及调剂补录的顺序。

第三块是工程化。数据库密码、管理员账号全部改成环境变量注入；
启动时不再自动执行带 `DROP TABLE` 的建表脚本，避免重启清空数据；
登录拦截器改成真正基于 Session 校验；另外配了 Docker Compose 和 GitHub Actions。

验证方面，除了单元测试，我还写了 2 个连接真实 MySQL 的集成测试，
覆盖「导入计划 → 导入志愿 → 投档 → 调剂 → 导出」完整链路。
另外我用示例数据在本地跑了一次真实业务：6862 名考生，计划总数 6666，
最后 6409 人按志愿录取、257 人调剂录取，两者相加正好等于计划总数，196 人退档，
没有任何专业超录，投档耗时大约 3.5 秒。

## 四、5 分钟版本（技术面展开）

### 1. 业务规则

平行志愿的核心是「分数优先、遵循志愿」：

- **分数优先**：所有考生按排位排序，排位高的先投档。
- **遵循志愿**：每个考生从第 1 志愿开始检查，命中第一个还有余额的专业就停止。
- **一次投档**：一旦投出，后续志愿不再检索。
- **调剂**：6 个志愿全部落空且服从调剂的考生进入调剂池，按排位补进缺额专业。

### 2. 代码结构

```
Controller  → 只做参数校验和结果包装
Service     → 事务、分页、状态校验、调用规则引擎
EnrollEngine→ 纯规则：投档与调剂
Mapper      → MyBatis XML 动态 SQL
```

我特意把规则抽出来，因为原来投档逻辑是 6 个 `if-else` 写在 Service 里，
既不好读也测不了。改成引擎之后，Service 里只剩下一句话：

```java
EnrollEngine.Match match = EnrollEngine.enroll(student, planById);
EnrollEngine.apply(student, match);
```

### 3. 性能考虑

- 考生按排位分页读取，每批 200 条，不会把全量考生加载进内存。
- 招生计划在投档前一次性查出来，转成 `Map<专业代号, 计划>`，志愿命中是 O(1)。
- 专业余额在内存里累加，投档结束后一次性回写 `realistic_student_count`，
  避免每条考生都去更新专业表。
- 实测 6862 名考生投档耗时约 3.5 秒。

### 4. 数据安全

- 建表脚本默认不执行，避免「重启即清库」。
- 账号密码全部走环境变量。
- 记录当前流程状态的 SQL 加了 `log_id` 作为次序依据，因为 `log_time` 只有秒级精度，
  同一秒写两条日志会导致状态读取不确定。

### 5. 测试策略

| 层次 | 手段 | 覆盖内容 |
| --- | --- | --- |
| 规则层 | 8 个 JUnit 单元测试 | 投档、顺延、调剂、退档、不超录 |
| 流程层 | 2 个 Spring Boot 集成测试（真实 MySQL） | 导入、投档、调剂、导出的完整链路 |
| 数据层 | SQL 一致性校验 | 不存在超录专业，计划数与录取数一致 |

---

## 五、高频面试问答

### Q1：为什么把录取算法单独抽成一个类？

投档规则属于业务规则，和数据库访问是两件不同的事。原来的写法把 6 个 `if-else`
和数据库查询混在一个方法里，修改规则要动 Service，测试还必须连数据库。
拆成 `EnrollEngine` 之后有三个好处：

1. 规则纯函数化，可以写秒级的单元测试，覆盖所有分支；
2. Service 只负责分页、事务和持久化，职责更单一；
3. 以后要做不同批次的投档规则（比如征集志愿），可以新增实现而不改现有逻辑。

### Q2：怎么保证不超录？

有两个层面的保障：

1. 规则层面：投档前判断 `realistic_student_count < plan_student_count` 才会命中，
   命中后立刻把已录人数加一，同一个专业在内存中的计数是连续的。
2. 数据层面：跑完之后我用 SQL 校验 `realistic_student_count > plan_student_count`
   的专业数，实测为 0。

### Q3：调剂为什么容易写错？

两个坑：

1. **缺额池的查询**。原来的 SQL 用内连接把专业表和考生表关联，
   结果是「从来没有学生投档的专业」不会出现在缺额池里。
   但这恰恰是最需要调剂的专业（一个人都没招到）。
   我改成 `LEFT JOIN`，并用 `ORDER BY bottom_rank IS NULL, bottom_rank`
   把有投档记录的专业排在前面，空专业排在后面。
2. **分页偏移量**。待调剂考生的条件是 `accepted_type = 0`，
   一旦被调剂成功就变成 7，记录会从结果集中消失。
   所以循环里**不能**推进 `start`，否则会跳过一部分考生。

### Q4：Excel 导入几万行会内存溢出吗？

不会。EasyExcel 是基于 SAX 的流式解析，不会把整个工作簿读进内存；
我再用一个 20 条一批的列表做批量插入，进一步降低内存占用和数据库往返次数。

> 这里还有一个我自己踩过的坑：监听器里的缓存列表我原来写成了 `static`，
> 多个导入请求会共享同一个列表，导致数据串号。
> EasyExcel 每次导入都会 new 一个监听器，所以缓存必须是实例字段。

### Q5：流程状态是怎么管的？

状态存在 `t_log` 表里，取最新一条记录的 `status` 字段，
分别是 `START(0)`、`WITHOUT_STUDENT(1)`、`FILE_READY(2)`、`ENROLLED(3)`、`ADJUSTED(4)`。
每执行一步都校验前置状态，比如没导入考生志愿就执行投档，会直接抛业务异常，
接口返回 `001` 和明确提示。

### Q6：为什么状态查询要按 `log_id` 排？

`log_time` 是 `datetime(0)`，只有秒级精度。授权导入和录取如果发生在同一秒，
只按 `log_time DESC` 排序时数据库返回哪一条是不确定的，会出现「刚录取完再查状态还是 2」。
加上 `log_id DESC` 作为次序依据后，后写入的记录一定排在前面。
这个问题是我跑集成测试时暴露出来的，也说明单纯靠人工点页面测不出来。

### Q7：登录和权限是怎么做的？

用 Spring MVC 的 `HandlerInterceptor`。登录成功后往 HttpSession 写一个标记，
拦截器检查这个标记，未登录直接返回 HTTP 401 和 `code=010`。
登录成功时会先把旧 Session 失效再新建，避免会话固定攻击。
静态资源、登录接口和 `/error` 做了放行，否则登录页自己都加载不出来。

> 早期版本的拦截器是直接 `return true`，等于没校验；
> 而且把 Session 对象存进了一个全局 `HashMap`，多用户会互相覆盖，Session 也不会被回收。

### Q8：这个项目的难点在哪？

难点不在 CRUD，而在规则和边界：

- 投档顺序必须严格按排位，且每个考生只投一次；
- 专业余额要在投档过程中实时变化，并且最后要和数据库一致；
- 调剂存在两个隐蔽坑（缺额池内连接、分页偏移量），写错会出现「计划没招满」或「漏调剂」；
- 数据规模到几千上万条时，分页和批量写库的取舍会直接影响耗时。

### Q9：如果数据量再涨十倍怎么办？

现在的实现是单事务批量投档。数据量再上一个量级，我会：

1. 投档按批次提交，并记录断点，支持断点续跑；
2. 把状态从「日志表取最新一条」改成独立的状态表 + 乐观锁；
3. 统计报表改成定时汇总到汇总表，避免每次全表聚合；
4. 用 `EXPLAIN` 确认投档和统计 SQL 走的是 `idx_rank` 与 `idx_mjr_rk`。

### Q10：前端是你写的吗？

管理台前端是 Vue 2 + Element UI + ECharts 实现的录取管理界面，
以构建后的静态资源形式集成在后端服务里，随应用一起启动。
我在这个项目里主要负责后端接口、投档规则、Excel 导入导出、数据库设计和工程化改造。

---

## 六、被问到不会的问题怎么答

1. 先复述问题，确认对方问的是不是自己理解的那件事。
2. 能答的部分直接说，并给出证据（代码位置、测试用例、实测数据）。
3. 不确定的部分明确说「这块我目前是按 X 理解的，没有做过 Y 场景的验证」。
4. 如果和项目相关，补一句「如果继续做，我会先做 Z 这件事」。

面试官更在意你**知道自己做了什么、不知道什么**，而不是全都答得完美。
